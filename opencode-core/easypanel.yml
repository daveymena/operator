# Referencia: la config principal está en ../easypanel.yml (raíz del repo).
# Este archivo se mantiene como documentación de los puertos y variables.
#
# Servicios en el contenedor:
#   serve.js          :21294  → Web UI + Chat API
#   agent-server.mjs  :21291  → WebSocket hub para PC Agents (Windows)
#   bridge-server.mjs :21295  → MiMoCode Bridge (conecta IA ↔ PC Agent)
#   web-operator/     :3001   → Web Operator API (stub)
#   mimo-mcp-server   :21296  → MCP Server
#   Xvfb + x11vnc     :5900/6080 → Pantalla virtual
#   skills scheduler  → Tareas programadas (preoperacional)
